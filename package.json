{
  "name": "severance-party",
  "version": "1.0.0",
  "description": "A blinking chapters web experience",
  "scripts": {
    "start": "live-server --port=3000 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
